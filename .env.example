# .env.example

# Projeto GCP (pode manter o nome do seu)
GCP_PROJECT=desafio-civitas-brt-ari

# Nome do bucket usado na pipeline
GCS_BUCKET=civitas-brt-data

GCS_ENV=dev

# Caminho local esperado da chave (substituir ao testar)
GOOGLE_APPLICATION_CREDENTIALS=/path/to/service-account-key.json